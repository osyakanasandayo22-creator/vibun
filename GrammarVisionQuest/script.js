const units = {
  "14 形容詞・副詞": [
    {
      "wordNo": 1,
      "word": "red",
      "meaning": "Point A-1〜3: 形容詞の限定用法（名詞を前後から修飾）",
      "detail": "【形容詞の限定用法（1〜3）】\n名詞を「前から」修飾する場合と、「名詞の直後の句」として後ろから修飾する場合がある。いずれも名詞句の中で名詞を特定・描写する働き（限定用法）。\n\n【本問】\nred は sweater の直前に置かれた前置修飾。「赤いセーター」と種類を絞る典型的な限定用法。",
      "sentence": "I bought a (   ) sweater.",
      "jp": "私は赤いセーターを買った。",
      "jpPartial": "赤い"
    },
    {
      "wordNo": 2,
      "word": "full",
      "meaning": "Point A-1〜3: 限定用法（後ろから修飾）",
      "detail": "【限定用法・後ろからの修飾】\nof people は形容詞 full とまとまり、train を後ろから説明する（＝「人でいっぱいの列車」）。\n\n【本問】\nfull of people は限定用法として train にかかる。意味的には which is full of people に近い。",
      "sentence": "We got on a train (   ) of people.",
      "jp": "私たちは人でいっぱいの列車に乗った。",
      "jpPartial": "人でいっぱいの"
    },
    {
      "wordNo": 3,
      "word": "new",
      "meaning": "Point A-1〜3: 限定用法",
      "detail": "【限定用法・不定代名詞＋形容詞】\nsomething / anything / nothing などの後ろに形容詞を置く型はよく出る（something new / nothing serious など）。\n\n【本問】\nnew は something の直後に置かれ「新しい何か」と限定している。",
      "sentence": "I want to try something (   ).",
      "jp": "私は何か新しいことをやってみたい。",
      "jpPartial": "新しい"
    },
    {
      "wordNo": 4,
      "word": "hungry",
      "meaning": "Point A-4: 叙述用法［SVC］補語としての形容詞",
      "detail": "【叙述用法（predicative）SVC】\n主語の状態・性質を述べる補語（C）として使う。S = C の関係（I'm hungry.＝ I am a hungry person ではなく「空腹状態だ」）。\n\n【本問】\nhungry は補語で主語 I の状態を述べる。",
      "sentence": "I'm (   ).",
      "jp": "私はお腹がすいている。",
      "jpPartial": "お腹がすいている"
    },
    {
      "wordNo": 5,
      "word": "open",
      "meaning": "Point A-5: 叙述用法［SVOC］O=Cの関係",
      "detail": "【叙述用法 SVOC】\nO = C の関係で目的語の状態を述べる（leave O C）。open は「開いた状態のまま」という結果を表す。\n\n【本問】\nthe door と open が対応し、ドアが開いたままであることを述べている。",
      "sentence": "He left the door (   ).",
      "jp": "彼はドアを開けたままにしておいた。",
      "jpPartial": "開けたままにしておいた"
    },
    {
      "wordNo": 6,
      "word": "elder",
      "meaning": "Point A-6: 限定用法しかない形容詞（elder など）",
      "detail": "【限定用法のみの形容詞】\nelder, former, latter, main などは名詞の前に置く用法に限られることが多い（*My sister is elder. のように叙述単独では使わない）。\n\n【本問】\nelder sister で「年上の姉」。比較級 older とは用法の決まりが異なるので混同注意。",
      "sentence": "My (   ) sister is a college student.",
      "jp": "私の姉は大学生だ。",
      "jpPartial": "姉"
    },
    {
      "wordNo": 7,
      "word": "alive",
      "meaning": "Point A-7: 叙述用法しかない形容詞（alive など）",
      "detail": "【叙述用法のみの形容詞】\nalive, asleep, awake, content, glad など「状態」を述べる語は補語として使われることが多い（限定の a glad boy は可能だが alive は叙述に多い）。\n\n【本問】\nalive and well は「生きていて元気だ」と状態を述べる。well はここでは形容詞「健康で」。",
      "sentence": "My grandmother is still (   ) and well.",
      "jp": "私の祖母はまだ存命で、元気です。",
      "jpPartial": "存命で、元気です"
    },
    {
      "wordNo": 8,
      "word": "present",
      "meaning": "Point A-8: 限定用法「現在の～」",
      "detail": "【限定と叙述で意味が変わる形容詞】\n限定用法と叙述用法で意味が変わる代表例（教材の表）:\n・present: 限定「現在の〜」／叙述「出席した」\n・certain: 限定「ある〜」／叙述「確信した」\n・late: 限定「最近の〜」／叙述「遅れた」\n・ill: 限定「悪い〜」／叙述「病気で」\n\n【本問】\nthe present situation の present は「現在の」＝限定用法。",
      "sentence": "What do you think of the (   ) situation?",
      "jp": "現在の状況についてあなたはどう思いますか。",
      "jpPartial": "現在の"
    },
    {
      "wordNo": 9,
      "word": "present",
      "meaning": "Point A-9: 叙述用法「出席した」",
      "detail": "【present の叙述用法】\nbe present at ～ で「〜に出席している」。限定の「現在の」とは別義（前問の表の叙述欄）。\n\n【本問】\nwas not present は「出席していなかった」。",
      "sentence": "He was not (   ) at the meeting.",
      "jp": "彼はその会合に出席していなかった。",
      "jpPartial": "出席していなかった"
    },
    {
      "wordNo": 10,
      "word": "convenient",
      "meaning": "Point A-10: 人が主語にならない形容詞（convenient など）",
      "detail": "【主語に人が来ない形容詞】\nconvenient, necessary, possible, impossible などは「It is convenient for you to ...」「My house is convenient for ...」のように物・事が主語になりやすい。\n\n【本問】\nMy house is convenient for commuting. は「家が便利」という英語として自然。",
      "sentence": "My house is (   ) for commuting.",
      "jp": "私の家は通勤に便利だ。",
      "jpPartial": "便利だ"
    },
    {
      "wordNo": 11,
      "word": "quickly",
      "meaning": "Point B-11: 様態を表す副詞",
      "detail": "【様態副詞】\n動作の仕方を表す（quickly, quietly, fluently など）。文位置は一般に動詞・目的語の近く。\n\n【本問】\nquickly は ate にかかり「速く食べた」。",
      "sentence": "I ate my breakfast (   ).",
      "jp": "私は急いで朝食をとった。",
      "jpPartial": "急いで"
    },
    {
      "wordNo": 12,
      "word": "here",
      "meaning": "Point B-12: 場所を表す副詞",
      "detail": "【場所副詞】\nhere / there / abroad などは場所を直接示す。\n\n【本問】\nhere は arrived の地点を示す。",
      "sentence": "We arrived (   ) this morning.",
      "jp": "私たちは今朝ここに着きました。",
      "jpPartial": "今朝ここに"
    },
    {
      "wordNo": 13,
      "word": "tomorrow",
      "meaning": "Point B-12・13: 時を表す副詞",
      "detail": "【時副詞】\ntomorrow, yesterday, tonight などは文全体の時間枠を示すことが多い。\n\n【本問】\nsee a doctor と時の情報 tomorrow が組み合わさる。",
      "sentence": "I'm going to see a doctor (   ).",
      "jp": "明日、医者に診てもらう予定です。",
      "jpPartial": "明日"
    },
    {
      "wordNo": 14,
      "word": "always",
      "meaning": "Point B-14: 頻度を表す副詞",
      "detail": "【頻度副詞】\nalways, usually, often, never などは be 動詞の後・一般動詞の前など位置の決まりがある。\n\n【本問】\nalways は busy の前（be 動詞の後）で「いつも忙しい」。",
      "sentence": "My mother is (   ) busy.",
      "jp": "母はいつも忙しい。",
      "jpPartial": "いつも"
    },
    {
      "wordNo": 15,
      "word": "almost",
      "meaning": "Point B-15: 程度を表す副詞",
      "detail": "【程度副詞】\nalmost, quite, really, very などは形容詞・副詞・動詞句を修飾する。\n\n【本問】\nalmost immediately で「ほぼすぐに」。程度を抑えつつ immediacy を強調。",
      "sentence": "I fell asleep (   ) immediately.",
      "jp": "私はほとんど一瞬で眠りに落ちた。",
      "jpPartial": "ほとんど"
    },
    {
      "wordNo": 16,
      "word": "probably",
      "meaning": "Point B-16: 文全体を修飾する副詞（確信度）",
      "detail": "【文修飾副詞（disjunct）】\nprobably, definitely, unfortunately などは文全体の見方（確信度・話者の態度）を表す。\n\n【本問】\nprobably は「たぶん」＝話者の推量の度合いを示し、will rain 全体にかかる。",
      "sentence": "It will (   ) rain tomorrow.",
      "jp": "明日はたぶん雨が降るだろう。",
      "jpPartial": "たぶん"
    }
  ],
  "4 比較③": [
    {
      "wordNo": 17,
      "word": "soon",
      "meaning": "Point A: as ＋原級＋ as possible（できるだけ～）",
      "detail": "【as ＋原級＋ as possible】\n「できるだけ〜」の定型。soon は「早く」の原級。\n\n【本問】\nas soon as possible は依頼文で頻出の決まり文句。",
      "sentence": "Please let me know as (   ) as possible.",
      "jp": "できるだけすぐに私に知らせてください。",
      "jpPartial": "すぐに"
    },
    {
      "wordNo": 18,
      "word": "ever",
      "meaning": "Point A: as ＋原級＋ as ever（相変わらず～）",
      "detail": "【as ＋原級＋ as ever】\n「いつも通り／相変わらず〜」の強調。\n\n【本問】\nas popular as ever＝「以前と同様に人気がある」。",
      "sentence": "The band is as popular as (   ).",
      "jp": "そのバンドは相変わらず人気がある。",
      "jpPartial": "相変わらず"
    },
    {
      "wordNo": 19,
      "word": "as",
      "meaning": "Point A: as many [much] as ＋数詞＋名詞",
      "detail": "【as many [much] as ＋数詞＋名詞】\n「（数・量が）〜もの…」と驚きや多さを込めて言う。\n\n【本問】\nAs many as five thousand people＝「なんと5千人もの」。",
      "sentence": "(   ) many as five thousand people took part in the campaign.",
      "jp": "5千人もの人たちがそのキャンペーンに参加した。",
      "jpPartial": "なんと5千人もの"
    },
    {
      "wordNo": 20,
      "word": "harder",
      "meaning": "Point B: (all) the ＋比較級 (for ～)",
      "detail": "【(all) the ＋比較級 (for ～)】\n理由・状況が加わることで「いっそう／ますます…」という含み。\n\n【本問】\nall the harder for the next exam＝「次の試験があるので一層勉強が必要」。",
      "sentence": "I need to study all the (   ) for the next exam.",
      "jp": "次の試験があるのでいっそう勉強する必要がある。",
      "jpPartial": "いっそう"
    },
    {
      "wordNo": 21,
      "word": "less",
      "meaning": "Point B: none the less (for ～)",
      "detail": "【none the less (for ～)】\n「それでもやはり」「〜にもかかわらず依然として」。\n\n【本問】\nfaults があっても好きさは変わらない、という譲歩のニュアンス。",
      "sentence": "I like Mike none the (   ) for his faults.",
      "jp": "マイクには欠点があるが、それでも私は彼が好きだ。",
      "jpPartial": "それでも"
    },
    {
      "wordNo": 22,
      "word": "much",
      "meaning": "Point B: much less ～（まして～ない）",
      "detail": "【much less ～】\n否定文などで「さらにその下位のことは言うまでもない＝まして〜ない」。\n\n【本問】\nused car すら無理 → new one は much less で強める。",
      "sentence": "I can't afford to buy a used car, (   ) less a new one.",
      "jp": "私は中古車を買う余裕はないし、まして新車は買えない。",
      "jpPartial": "まして"
    },
    {
      "wordNo": 23,
      "word": "most",
      "meaning": "Point C: at (the) most",
      "detail": "【at (the) most】\n数量の上限を言う（せいぜい／多くても）。\n\n【本問】\n50人が上限という見積もり。",
      "sentence": "There were at the (   ) 50 people in the concert hall.",
      "jp": "コンサートホールには多くても50人しかいなかった。",
      "jpPartial": "多くても"
    },
    {
      "wordNo": 24,
      "word": "best",
      "meaning": "Point C: at best",
      "detail": "【at best】\n「せいぜい」「最高でも（それ以下の可能性が高い）」。\n\n【本問】\n試合の帰結としては引き分けがやっと、という見通し。",
      "sentence": "The game will end in a draw at (   ).",
      "jp": "その試合はせいぜい引き分けだろう。",
      "jpPartial": "せいぜい"
    },
    {
      "wordNo": 25,
      "word": "best",
      "meaning": "Point C: at one's best",
      "detail": "【at one's best】\n「最高の調子で／ベストの状態で」。所有代名詞で人が変わる（at my best など）。\n\n【本問】\nwasn't at my best＝「体調・パフォーマンスが最高ではなかった」。",
      "sentence": "It was a tough match, and I wasn't at my (   ).",
      "jp": "それは厳しい試合だったし、私は最高の状態というわけではなかった。",
      "jpPartial": "最高の調子ではなかった"
    },
    {
      "wordNo": 26,
      "word": "most",
      "meaning": "Point C: make the most of ～",
      "detail": "【make the most of ～】\n「〜を最大限に活用する」。most は最上級の名詞的用法。\n\n【本問】\nchance を逃さず使い切る、という励まし。",
      "sentence": "We must make the (   ) of this chance.",
      "jp": "私たちはこの機会を最大限に利用しなければならない。",
      "jpPartial": "最大限に"
    },
    {
      "wordNo": 27,
      "word": "not",
      "meaning": "Point D: not more than ～（多くても～ ≦）",
      "detail": "【not more than ～】\n「多くても〜」数量の上限（≦）。at most に近い客観的な上限表現。\n\n【本問】\n10ドル以下かもしれない、という上限の意味合い。",
      "sentence": "Tom has (   ) more than 10 dollars.",
      "jp": "トムは多くても10ドルしか持っていない。",
      "jpPartial": "多くても"
    },
    {
      "wordNo": 28,
      "word": "not",
      "meaning": "Point D: not less than ～（少なくとも～ ≧）",
      "detail": "【not less than ～】\n「少なくとも〜」数量の下限（≧）。at least に近い。\n\n【本問】\n10ドル以上はある、という下限の意味合い。",
      "sentence": "Tom has (   ) less than 10 dollars.",
      "jp": "トムは少なくとも10ドルは持っている。",
      "jpPartial": "少なくとも"
    },
    {
      "wordNo": 29,
      "word": "no",
      "meaning": "Point D: no more than ～（～だけ／少ない気持ち）",
      "detail": "【no more than ～】\n「わずか〜だけ」＝数量はその程度に過ぎない（only の気持ち）。\n\n【本問】\n10ドルしかない、という「少なさ」の含み。",
      "sentence": "Tom has (   ) more than 10 dollars.",
      "jp": "トムはたった10ドルしか持っていない。",
      "jpPartial": "たった"
    },
    {
      "wordNo": 30,
      "word": "no",
      "meaning": "Point D: no less than ～（～も／多い気持ち）",
      "detail": "【no less than ～】\n「〜も（多く）」＝数量・程度が予想以上（as many/much as の気持ち）。\n\n【本問】\n10ドルも持っている、という「多さ／意外さ」の含み。",
      "sentence": "Tom has (   ) less than 10 dollars.",
      "jp": "トムは10ドルも持っている。",
      "jpPartial": "10ドルも"
    }
  ],
  "7 同格・無生物主語・名詞構文": [
    {
      "wordNo": 31,
      "word": "that",
      "meaning": "Point A: 同格の that（fact, news, idea など）",
      "detail": "【同格の that】\nfact, news, rumor, idea, proposal, demand など「内容を後で説明できる名詞」の直後に that 節が続き、名詞と同格関係になる。\n\n【本問】\nthe fact that ...＝「〜という事実」。that 節が fact の中身を具体化。",
      "sentence": "He faced the fact (   ) he would have to try again.",
      "jp": "もう一度挑戦しなければならないという事実に彼は直面した。",
      "jpPartial": "という"
    },
    {
      "wordNo": 32,
      "word": "makes",
      "meaning": "Point B: S make O do → 日本語では副詞的に",
      "detail": "【無生物主語＋ make O do】\n直訳「写真が私に笑わせる」は不自然 → 日本語では「〜を見ると笑ってしまう」のように主語を副詞的に訳すのが定石。\n\n【本問】\nmake の O あと原形動詞 laugh。知覚・反応の表現として覚える。",
      "sentence": "This picture (   ) me laugh.",
      "jp": "この写真を見ると私は笑ってしまう。",
      "jpPartial": "笑ってしまう"
    },
    {
      "wordNo": 33,
      "word": "reminds",
      "meaning": "Point B: S remind O of ～",
      "detail": "【remind O of ～】\n「Oに〜を思い出させる」→ 日本語では「〜を聞くと思い出す」型に転写しやすい。\n\n【本問】\nof の後ろが想起される内容（my childhood）。",
      "sentence": "This song (   ) me of my childhood.",
      "jp": "この歌を聞くと私は自分の子ども時代を思い出す。",
      "jpPartial": "思い出す"
    },
    {
      "wordNo": 34,
      "word": "allows",
      "meaning": "Point B: S allow O to do",
      "detail": "【allow O to do】\n許可の字面だが、無生物主語では「〜があって O は〜できる」＝条件・手段のニュアンスになりやすい。\n\n【本問】\nthe internet allows us to connect...＝「ネットのおかげでつながれる」。",
      "sentence": "The internet (   ) us to connect with people all over the world.",
      "jp": "インターネットのおかげで世界中の人々とつながることができる。",
      "jpPartial": "つながることができる"
    },
    {
      "wordNo": 35,
      "word": "enables",
      "meaning": "Point B: S enable O to do",
      "detail": "【enable O to do】\n「可能にする」。allow と同様、無生物主語で「機能・ツールが可能にする」が自然。\n\n【本問】\nsoftware enables you to create...",
      "sentence": "This software (   ) you to create websites easily.",
      "jp": "このソフトのおかげで簡単にウェブサイトを作成することができる。",
      "jpPartial": "作成することができる"
    },
    {
      "wordNo": 36,
      "word": "prevented",
      "meaning": "Point B: S prevent O from doing",
      "detail": "【prevent O from doing】\n「Oが〜するのを妨げる」→ 自然訳「〜のせいで〜できない」。\n\n【本問】\nfrom に -ing（going）。",
      "sentence": "The rain (   ) us from going to the beach.",
      "jp": "雨のために私たちはビーチに行けなかった。",
      "jpPartial": "行けなかった"
    },
    {
      "wordNo": 37,
      "word": "takes",
      "meaning": "Point B: S take O to ～",
      "detail": "【take O to ～】\n「連れて行く」だが、バスなどでは「乗れば到着する」という習慣表現になりやすい。\n\n【本問】\nThe school bus takes children to school.",
      "sentence": "The school bus (   ) children to school.",
      "jp": "スクールバスに乗って子どもたちは学校へ行く。",
      "jpPartial": "学校へ行く"
    },
    {
      "wordNo": 38,
      "word": "tells",
      "meaning": "Point B: S tell O ～",
      "detail": "【tell O wh- 節／不定詞】\n人が主語なら「教える」が、map などでは「見ればわかる」に訳すのが自然。\n\n【本問】\ntells you how to get...",
      "sentence": "The map (   ) you how to get to the station.",
      "jp": "地図を見れば駅への行き方がわかる。",
      "jpPartial": "わかる"
    },
    {
      "wordNo": 39,
      "word": "rest",
      "meaning": "Point C: take [have] a/an ＋名詞",
      "detail": "【take [have] a/an ＋名詞】\n動作を名詞で言い換える表現（take a rest ≒ rest）。\n\n【本問】\ntake a rest は会話でよく使う提案表現。",
      "sentence": "\"Why don't you take a (   )?\" \"All right.\"",
      "jp": "「ひと休みしたらどうですか。」「わかりました。」",
      "jpPartial": "ひと休み"
    },
    {
      "wordNo": 40,
      "word": "singer",
      "meaning": "Point C: a/an ＋形容詞＋～する人",
      "detail": "【a good singer 型】\n「歌が上手い人」＝ She sings well. を名詞構文で言い換えたもの（動作を表す名詞化）。\n\n【本問】\na good swimmer / a good writer なども同型。",
      "sentence": "She is a good (   ).",
      "jp": "彼女は歌が上手だ。",
      "jpPartial": "歌が上手だ"
    },
    {
      "wordNo": 41,
      "word": "of",
      "meaning": "Point C: 〈所有格＋名詞〉意味上の主語・目的語",
      "detail": "【所有格＋名詞】\nthe news of his death の of は「内容・属性」を示し、that he had died に近い同格の働きに触れることもある。\n\n【本問】\nnews of ～＝「〜というニュース」。",
      "sentence": "I was shocked at the news (   ) his death.",
      "jp": "彼の死の知らせに私は衝撃を受けた。",
      "jpPartial": "彼の死の"
    }
  ],
  "9-1 前置詞①": [
    {
      "wordNo": 42,
      "word": "at",
      "meaning": "Point A: at［場所の一点］",
      "detail": "【at：場所の“点”】\n小さな場、拠点、停留所など「点」で捉えられる場所に at。\n\n【本問】\nat the bus stop は典型。",
      "sentence": "We were waiting (   ) the bus stop.",
      "jp": "私たちはバス停で待っていた。",
      "jpPartial": "バス停で"
    },
    {
      "wordNo": 43,
      "word": "at",
      "meaning": "Point A: at［時の一点］",
      "detail": "【at：時刻の“点”】\n「3時に」など時刻の一点に at。\n\n【本問】\nat three。",
      "sentence": "I'll see you (   ) three.",
      "jp": "3時にお会いしましょう。",
      "jpPartial": "3時に"
    },
    {
      "wordNo": 44,
      "word": "in",
      "meaning": "Point A: in［空間の中］",
      "detail": "【in：空間の内部】\n囲まれた空間の中に in。\n\n【本問】\nin the room。",
      "sentence": "There was no one (   ) the room.",
      "jp": "部屋には誰もいなかった。",
      "jpPartial": "部屋に"
    },
    {
      "wordNo": 45,
      "word": "in",
      "meaning": "Point A: in［幅のある時間］",
      "detail": "【in：長い時間の枠】\n年・月・季節など「幅」を持つ時間に in。\n\n【本問】\nin 2015。",
      "sentence": "I first met him (   ) 2015.",
      "jp": "2015年に初めて彼に会った。",
      "jpPartial": "2015年に"
    },
    {
      "wordNo": 46,
      "word": "in",
      "meaning": "Point A: in［所要時間・後に］",
      "detail": "【in：経過時間（あとどれくらいで）】\n「（今から）10分後に戻る」は in ten minutes。\n\n【本問】\n未来の所要／経過に in。",
      "sentence": "I'll be back (   ) ten minutes.",
      "jp": "10分後に戻ってきます。",
      "jpPartial": "10分後に"
    },
    {
      "wordNo": 47,
      "word": "on",
      "meaning": "Point A: on［接触］",
      "detail": "【on：接触面】\n表面に接触して載る／置く on。\n\n【本問】\non the table。",
      "sentence": "He put the book (   ) the table.",
      "jp": "彼は本をテーブルに置いた。",
      "jpPartial": "テーブルに"
    },
    {
      "wordNo": 48,
      "word": "on",
      "meaning": "Point A: on［曜日など］",
      "detail": "【on：曜日】\non Monday / on Christmas Day など。\n\n【本問】\non Monday。",
      "sentence": "I'll see you (   ) Monday.",
      "jp": "月曜日にお会いしましょう。",
      "jpPartial": "月曜日に"
    },
    {
      "wordNo": 49,
      "word": "from",
      "meaning": "Point B: from［起点］",
      "detail": "【from：起点】\n空間・時間の出発点。\n\n【本問】\nfrom Tokyo to Kyoto。",
      "sentence": "It takes about two hours (   ) Tokyo to Kyoto.",
      "jp": "東京から京都まで約2時間かかります。",
      "jpPartial": "東京から"
    },
    {
      "wordNo": 50,
      "word": "from",
      "meaning": "Point B: from〜to〜",
      "detail": "【from … to …】\n時間の範囲も同様。\n\n【本問】\nfrom 8:30 a.m. to 5:00 p.m.。",
      "sentence": "I'm at school (   ) 8:30 a.m. to 5:00 p.m.",
      "jp": "私は午前8時30分から午後5時まで学校にいます。",
      "jpPartial": "午前8時30分から"
    },
    {
      "wordNo": 51,
      "word": "for",
      "meaning": "Point B: for［方向］",
      "detail": "【for：行き先（出発動詞と）】\nleave for London＝「ロンドンへ向けて出発する」。\n\n【本問】\nleave for。",
      "sentence": "He left (   ) London yesterday.",
      "jp": "彼は昨日ロンドンへたちました。",
      "jpPartial": "ロンドンへ"
    },
    {
      "wordNo": 52,
      "word": "for",
      "meaning": "Point B: for［期間］",
      "detail": "【for：継続の期間】\n「2時間勉強した」は for two hours。\n\n【本問】\nfor two hours。",
      "sentence": "I studied (   ) two hours last night.",
      "jp": "私は昨夜、2時間勉強した。",
      "jpPartial": "2時間"
    },
    {
      "wordNo": 53,
      "word": "into",
      "meaning": "Point C: into",
      "detail": "【into：中へ入る方向】\nin より「入る動き」が強い。\n\n【本問】\njumped into the box。",
      "sentence": "A frog jumped (   ) the box.",
      "jp": "カエルが箱の中へ飛び込んだ。",
      "jpPartial": "箱の中へ"
    },
    {
      "wordNo": 54,
      "word": "out of",
      "meaning": "Point C: out of",
      "detail": "【out of：中から外へ】\ninto の反対方向。\n\n【本問】\njumped out of the box。",
      "sentence": "A frog jumped (   ) the box.",
      "jp": "カエルが箱の中から飛び出した。",
      "jpPartial": "箱の中から"
    },
    {
      "wordNo": 55,
      "word": "along",
      "meaning": "Point C: along",
      "detail": "【along：沿って】\n線・縁に沿う動き。\n\n【本問】\nalong the street（通りに沿って）。",
      "sentence": "They walked (   ) the street toward the park.",
      "jp": "彼らは通りに沿って歩いた。",
      "jpPartial": "通りに沿って"
    },
    {
      "wordNo": 56,
      "word": "across",
      "meaning": "Point C: across",
      "detail": "【across：横断】\n平面を「越えて行く」イメージ。\n\n【本問】\nacross the street（通りを横切る）。",
      "sentence": "They walked (   ) the street to the other side.",
      "jp": "彼らは通りを歩いて横切った。",
      "jpPartial": "通りを横切って"
    },
    {
      "wordNo": 57,
      "word": "through",
      "meaning": "Point C: through",
      "detail": "【through：内部を通り抜ける】\nトンネル・森など「内部経路」。\n\n【本問】\nthrough the tunnel。",
      "sentence": "The train went (   ) the tunnel.",
      "jp": "列車はトンネルを通り抜けた。",
      "jpPartial": "トンネルを通り抜けて"
    },
    {
      "wordNo": 58,
      "word": "over",
      "meaning": "Point D: over",
      "detail": "【over：越える／上の方へ】\nジャンプして上を越える over。\n\n【本問】\njumped over the table。",
      "sentence": "The cat jumped (   ) the table.",
      "jp": "その猫がテーブルを跳び越えた。",
      "jpPartial": "テーブルを跳び越えて"
    },
    {
      "wordNo": 59,
      "word": "under",
      "meaning": "Point D: under",
      "detail": "【under：真下（接触もありうる）】\n机の下など。\n\n【本問】\nunder the table。",
      "sentence": "The boy was hiding (   ) the table.",
      "jp": "その少年はテーブルの下に隠れていた。",
      "jpPartial": "テーブルの下に"
    },
    {
      "wordNo": 60,
      "word": "above",
      "meaning": "Point D: above",
      "detail": "【above：基準より上（非接触も）】\nbelow と対になる「より上」。\n\n【本問】\nabove the horizon。",
      "sentence": "The sun rose (   ) the horizon.",
      "jp": "太陽が地平線の上に昇った。",
      "jpPartial": "地平線の上に"
    },
    {
      "wordNo": 61,
      "word": "below",
      "meaning": "Point D: below",
      "detail": "【below：基準より下】\n海面より低い below sea level は定番表現。\n\n【本問】\nbelow sea level。",
      "sentence": "This place is (   ) sea level.",
      "jp": "この場所は海面より低い。",
      "jpPartial": "海面より低い"
    }
  ],
  "9-2 前置詞②": [
    {
      "wordNo": 62,
      "word": "in front of",
      "meaning": "Point A: in front of",
      "detail": "【in front of】\n「〜の前方（外側）」。behind の対。\n\n【本問】\nin front of the station。",
      "sentence": "She was waiting (   ) the station.",
      "jp": "彼女は駅の前で待っていた。",
      "jpPartial": "駅の前で"
    },
    {
      "wordNo": 63,
      "word": "behind",
      "meaning": "Point A: behind",
      "detail": "【behind】\n「〜の後ろ側」。\n\n【本問】\nbehind the station。",
      "sentence": "There is a small park (   ) the station.",
      "jp": "駅の裏に小さな公園がある。",
      "jpPartial": "駅の裏に"
    },
    {
      "wordNo": 64,
      "word": "near",
      "meaning": "Point A: near",
      "detail": "【near】\n近接だが at ほど「点」ではない。\n\n【本問】\nnear my house。",
      "sentence": "I went to the supermarket (   ) my house.",
      "jp": "私は家の近くのスーパーに行った。",
      "jpPartial": "家の近くの"
    },
    {
      "wordNo": 65,
      "word": "around",
      "meaning": "Point A: around",
      "detail": "【around】\n周囲を取り囲む／周回。\n\n【本問】\naround the sun。",
      "sentence": "The earth goes (   ) the sun.",
      "jp": "地球は太陽の周りを回る。",
      "jpPartial": "太陽の周りを"
    },
    {
      "wordNo": 66,
      "word": "between",
      "meaning": "Point A: between（2つの間）",
      "detail": "【between】\n「ちょうど2つ」を想定した「の間」。\n\n【本問】\nbetween the trees（教材のニュアンス：2本の間）。",
      "sentence": "There is a small house (   ) the trees.",
      "jp": "（2本の）木の間に小さな家がある。",
      "jpPartial": "木の間に"
    },
    {
      "wordNo": 67,
      "word": "among",
      "meaning": "Point A: among（3つ以上の間）",
      "detail": "【among】\n「3つ以上の集まりの中のいずれか」。\n\n【本問】\namong the trees（木々のなか）。between との対比が試験でよく出る。",
      "sentence": "There is a small house (   ) the trees.",
      "jp": "（3本以上の）木々の間に小さな家がある。",
      "jpPartial": "木々の間に"
    },
    {
      "wordNo": 68,
      "word": "before",
      "meaning": "Point B: before",
      "detail": "【before】\n基準時より前。\n\n【本問】\nbefore dinner。",
      "sentence": "I finished my homework (   ) dinner.",
      "jp": "私は夕食前に宿題を終えた。",
      "jpPartial": "夕食前に"
    },
    {
      "wordNo": 69,
      "word": "after",
      "meaning": "Point B: after",
      "detail": "【after】\n基準時より後。\n\n【本問】\nafter dinner。",
      "sentence": "I'll do my homework (   ) dinner.",
      "jp": "私は夕食後に宿題をするつもりだ。",
      "jpPartial": "夕食後に"
    },
    {
      "wordNo": 70,
      "word": "since",
      "meaning": "Point B: since",
      "detail": "【since】\n「〜以来（ずっと）」継続の始点。\n\n【本問】\nsince this morning（今朝から降り続けている）。",
      "sentence": "It has been raining (   ) this morning.",
      "jp": "今朝からずっと雨が降り続いている。",
      "jpPartial": "今朝から"
    },
    {
      "wordNo": 71,
      "word": "until",
      "meaning": "Point B: until [till]",
      "detail": "【until / till】\n動作・状態が続く「終わりの時点」。\n\n【本問】\nopen until 10 p.m.。",
      "sentence": "The store is open (   ) 10 p.m.",
      "jp": "その店は夜10時まで開いている。",
      "jpPartial": "夜10時まで"
    },
    {
      "wordNo": 72,
      "word": "during",
      "meaning": "Point B: during",
      "detail": "【during】\n「〜の期間の中で（起こった出来事）」。\n\n【本問】\nduring the summer vacation。",
      "sentence": "We visited Korea (   ) the summer vacation.",
      "jp": "私たちは夏休みの間に韓国を訪れた。",
      "jpPartial": "夏休みの間に"
    },
    {
      "wordNo": 73,
      "word": "by",
      "meaning": "Point C: by［そばに］",
      "detail": "【by：そば】\n「そばに座る」 sit by me。\n\n【本問】\nby me。",
      "sentence": "Come and sit (   ) me.",
      "jp": "こっちへ来て私のそばに座りなさい。",
      "jpPartial": "私のそばに"
    },
    {
      "wordNo": 74,
      "word": "by",
      "meaning": "Point C: by［期限までに］",
      "detail": "【by：期限】\n「〜までに」完了・到達の期限。\n\n【本問】\nby nine。",
      "sentence": "I have to be home (   ) nine.",
      "jp": "私は9時までに家に帰らなければならない。",
      "jpPartial": "9時までに"
    },
    {
      "wordNo": 75,
      "word": "with",
      "meaning": "Point C: with［同伴］",
      "detail": "【with：同伴】\n「〜と一緒に」。\n\n【本問】\nwith my friends。",
      "sentence": "I went camping (   ) my friends last weekend.",
      "jp": "先週末、友人たちとキャンプに行った。",
      "jpPartial": "友人たちと"
    },
    {
      "wordNo": 76,
      "word": "of",
      "meaning": "Point C: of［所属］",
      "detail": "【of：所属・部類】\na member of the team＝「チームの一員」。\n\n【本問】\nmember of the soccer team。",
      "sentence": "He is a member (   ) the soccer team.",
      "jp": "彼はサッカーチームの一員です。",
      "jpPartial": "サッカーチームの"
    },
    {
      "wordNo": 77,
      "word": "because of",
      "meaning": "Point D: because of ～",
      "detail": "【because of】\n名詞句の理由（文の理由なら because 節）。\n\n【本問】\nbecause of the rain。",
      "sentence": "The game was postponed (   ) the rain.",
      "jp": "雨のためにその試合は延期された。",
      "jpPartial": "雨のために"
    },
    {
      "wordNo": 78,
      "word": "instead of",
      "meaning": "Point D: instead of ～",
      "detail": "【instead of】\n「〜の代わりに」。\n\n【本問】\ninstead of my sister。",
      "sentence": "Can I go to the party (   ) my sister?",
      "jp": "姉の代わりに私がパーティーに行ってもいいですか。",
      "jpPartial": "姉の代わりに"
    },
    {
      "wordNo": 79,
      "word": "in spite of",
      "meaning": "Point D: in spite of ～",
      "detail": "【in spite of】\n「〜にもかかわらず」（名詞／動名詞）。although とは品詞が違う。\n\n【本問】\nIn spite of the rain（文頭に置ける）。",
      "sentence": "(   ) the rain, they went to the beach.",
      "jp": "雨にもかかわらず、彼らは海辺へ行った。",
      "jpPartial": "雨にもかかわらず"
    },
    {
      "wordNo": 80,
      "word": "according to",
      "meaning": "Point D: according to ～",
      "detail": "【according to】\n「〜によれば」（情報源）。\n\n【本問】\nAccording to the weather forecast。",
      "sentence": "(   ) the weather forecast, it will be fine tomorrow.",
      "jp": "天気予報によると、明日は晴れるだろう。",
      "jpPartial": "天気予報によると"
    }
  ],
  "10 群動詞": [
    {
      "wordNo": 81,
      "word": "out",
      "meaning": "Point A: break out",
      "detail": "【break out】\n戦争・火事・病気などが「起こる」。\n\n【本問】\nThe war broke out。",
      "sentence": "The Second World War broke (   ) in 1939.",
      "jp": "第二次世界大戦は1939年に起こった。",
      "jpPartial": "起こった"
    },
    {
      "wordNo": 82,
      "word": "away",
      "meaning": "Point A: pass away",
      "detail": "【pass away】\n婉曲表現で「亡くなる」（die より丁寧な場面で）。",
      "sentence": "Steve Jobs passed (   ) on October 5, 2011.",
      "jp": "スティーブ・ジョブズは2011年10月5日に亡くなった。",
      "jpPartial": "亡くなった"
    },
    {
      "wordNo": 83,
      "word": "up",
      "meaning": "Point A: show [turn] up",
      "detail": "【show up / turn up】\n「現れる・やって来る」。\n\n【本問】\nshowed up at the press conference。",
      "sentence": "The leading actor showed (   ) at the press conference.",
      "jp": "主演俳優が記者会見に現れた。",
      "jpPartial": "現れた"
    },
    {
      "wordNo": 84,
      "word": "up",
      "meaning": "Point B: bring up",
      "detail": "【bring up】\n子どもを「育てる」、話題を「出す」など多義だがここは育児。\n\n【本問】\nbrought up three children。",
      "sentence": "She brought (   ) three children.",
      "jp": "彼女は3人の子どもを育て上げた。",
      "jpPartial": "育て上げた"
    },
    {
      "wordNo": 85,
      "word": "off",
      "meaning": "Point B: call off",
      "detail": "【call off】\n「中止する」≒ cancel。\n\n【本問】\ncalled off the trip。",
      "sentence": "My family called (   ) the trip.",
      "jp": "私の家族は旅行を中止した。",
      "jpPartial": "中止した"
    },
    {
      "wordNo": 86,
      "word": "for",
      "meaning": "Point B: call for",
      "detail": "【call for】\n「要求する」「必要とする」。\n\n【本問】\ncall for the release of...。",
      "sentence": "They called (   ) the release of the hostages.",
      "jp": "彼らは人質の解放を求めた。",
      "jpPartial": "人質の解放を求めた"
    },
    {
      "wordNo": 87,
      "word": "across",
      "meaning": "Point B: come across",
      "detail": "【come across】\n「偶然出会う／偶然見つける」。\n\n【本問】\ncame across Tom。",
      "sentence": "I came (   ) Tom on the train.",
      "jp": "私は列車でトムと偶然出会った。",
      "jpPartial": "偶然出会った"
    },
    {
      "wordNo": 88,
      "word": "into",
      "meaning": "Point B: look into",
      "detail": "【look into】\n「調査する」≒ investigate。\n\n【本問】\nlooking into the murder case。",
      "sentence": "He is looking (   ) the murder case.",
      "jp": "彼はその殺人事件を調査している。",
      "jpPartial": "調査している"
    },
    {
      "wordNo": 89,
      "word": "up with",
      "meaning": "Point B: catch up with",
      "detail": "【catch up with】\n「追いつく」。\n\n【本問】\ncaught up with him。",
      "sentence": "I caught (   ) him at last.",
      "jp": "私はようやく彼に追いついた。",
      "jpPartial": "追いついた"
    },
    {
      "wordNo": 90,
      "word": "with",
      "meaning": "Point B: get along with",
      "detail": "【get along with】\n「仲良くやる」「うまくやっていく」。\n\n【本問】\nget along with her（空所は with）。",
      "sentence": "John will get along (   ) her.",
      "jp": "ジョンは彼女と仲良くやっていくでしょう。",
      "jpPartial": "仲良くやっていく"
    },
    {
      "wordNo": 91,
      "word": "for",
      "meaning": "Point B: make up for",
      "detail": "【make up for】\n「埋め合わせをする」。\n\n【本問】\nmake up for the loss。",
      "sentence": "He tried to make up (   ) the loss.",
      "jp": "彼は損失の埋め合わせをしようとした。",
      "jpPartial": "埋め合わせをしようとした"
    },
    {
      "wordNo": 92,
      "word": "fault",
      "meaning": "Point B: find fault with",
      "detail": "【find fault with】\n「あらを探す」。\n\n【本問】\nfinds fault with everything（空所は fault）。",
      "sentence": "He finds (   ) with everything.",
      "jp": "彼は何でもあら探しをする。",
      "jpPartial": "あら探しをする"
    },
    {
      "wordNo": 93,
      "word": "use of",
      "meaning": "Point B: make use of",
      "detail": "【make use of】\n「利用する・活用する」。\n\n【本問】\nmade use of her experience。",
      "sentence": "She made (   ) her experience.",
      "jp": "彼女は自分の経験を生かした。",
      "jpPartial": "生かした"
    }
  ],
  "12 名詞・冠詞": [
    {
      "wordNo": 94,
      "word": "five",
      "meaning": "Point A: 数えられる名詞 C",
      "detail": "【可算名詞 C】\n数えられる普通名詞。数詞と共に使える。\n\n【本問】\nfive cats。",
      "sentence": "There were (   ) cats in the box.",
      "jp": "箱の中に猫が5匹いた。",
      "jpPartial": "5匹"
    },
    {
      "wordNo": 95,
      "word": "families",
      "meaning": "Point A: 数えられる集合名詞 C",
      "detail": "【可算の集合名詞 family など】\n文脈により単数扱い／複数扱いがあり得るが、a lot of families は「家族たち」。\n\n【本問】\nfamilies visit...。",
      "sentence": "A lot of (   ) visit the zoo on weekends.",
      "jp": "多くの家族が週末に動物園を訪れる。",
      "jpPartial": "家族が"
    },
    {
      "wordNo": 96,
      "word": "police",
      "meaning": "Point A: 数えられない集合名詞 U",
      "detail": "【police など不可算扱いの集合】\nthe police are のように複数動詞になりやすい。\n\n【本問】\nThe police are looking...。",
      "sentence": "The (   ) are looking for the man.",
      "jp": "警察はその男を捜している。",
      "jpPartial": "警察"
    },
    {
      "wordNo": 97,
      "word": "meat",
      "meaning": "Point A: 物質名詞 U",
      "detail": "【物質名詞 U】\nmeat は不可算。数えるなら a piece of meat など。\n\n【本問】\nThis meat is tender。",
      "sentence": "This (   ) is very tender.",
      "jp": "この肉はとても柔らかい。",
      "jpPartial": "肉"
    },
    {
      "wordNo": 98,
      "word": "Honesty",
      "meaning": "Point A: 抽象名詞 U",
      "detail": "【抽象名詞 honesty】\n不可算として扱われやすい概念名詞。\n\n【本問】\nHonesty is the best policy。",
      "sentence": "(   ) is the best policy.",
      "jp": "正直は最善の策。（ことわざ）",
      "jpPartial": "正直"
    },
    {
      "wordNo": 99,
      "word": "piece",
      "meaning": "Point A: a piece of などで数える",
      "detail": "【advice など不可算＋piece】\nadvice は不可算なので a piece of advice で「1つの助言」。\n\n【本問】\na piece of advice。",
      "sentence": "He gave me a (   ) of advice.",
      "jp": "彼は私に助言を1つしてくれた。",
      "jpPartial": "助言を1つ"
    },
    {
      "wordNo": 100,
      "word": "watch",
      "meaning": "Point B-7: 初出の名詞に a/an",
      "detail": "【不定冠詞 a：初出】\n初めて出る名詞に a が付く典型。\n\n【本問】\na new watch。",
      "sentence": "I bought a new (   ) today.",
      "jp": "今日は新しい腕時計を買った。",
      "jpPartial": "腕時計"
    },
    {
      "wordNo": 101,
      "word": "week",
      "meaning": "Point B-8: one と同じ意味の a/an",
      "detail": "【a week＝one week】\n期間の「1週間」を a で表す。\n\n【本問】\nfor a week。",
      "sentence": "I stayed in Hokkaido for a (   ).",
      "jp": "私は北海道に1週間、滞在した。",
      "jpPartial": "1週間"
    },
    {
      "wordNo": 102,
      "word": "hour",
      "meaning": "Point B-9: 単位ごとの a/an",
      "detail": "【an hour】\nhour は母音始まりなので an。\n\n【本問】\nfor an hour。",
      "sentence": "She runs for an (   ) three days a week.",
      "jp": "彼女は週に3日、1時間走っている。",
      "jpPartial": "1時間"
    },
    {
      "wordNo": 103,
      "word": "a",
      "meaning": "Point B-10: 初出 a → 再出 the",
      "detail": "【a dog → the dog】\n初出は a、同じ個体の再出は the で照応。\n\n【本問】\nIf you pet a dog, the dog will...。",
      "sentence": "If you pet (   ) dog, the dog will like you.",
      "jp": "犬を優しくなでれば、その犬はあなたを気に入るでしょう。",
      "jpPartial": "ある"
    },
    {
      "wordNo": 104,
      "word": "the",
      "meaning": "Point B-11: 状況から特定される the",
      "detail": "【状況で特定される the】\nその場のドアと聞き手が共有している対象。\n\n【本問】\nOpen the door。",
      "sentence": "\"Open (   ) door, please.\" \"OK.\"",
      "jp": "「ドアを開けてください。」「いいですよ。」",
      "jpPartial": "ドアを"
    },
    {
      "wordNo": 105,
      "word": "the",
      "meaning": "Point B-12: 唯一のものに the",
      "detail": "【唯一のもの the】\nthe sun / the moon / the earth など。\n\n【本問】\nThe sun is strong today。",
      "sentence": "(   ) sun is strong today.",
      "jp": "今日は日差しが強い。",
      "jpPartial": "その"
    },
    {
      "wordNo": 106,
      "word": "coffee",
      "meaning": "Point C-13: 不可算名詞に a/an は付かない",
      "detail": "【不可算 coffee】\n総称としての coffee には a/an を付けない（a coffee＝「1杯のコーヒー」は別用法）。\n\n【本問】\nDo you like coffee?",
      "sentence": "Do you like (   )?",
      "jp": "コーヒーは好きですか。",
      "jpPartial": "コーヒー"
    },
    {
      "wordNo": 107,
      "word": "school",
      "meaning": "Point C-14: 機能としての学校は無冠詞",
      "detail": "【go to school（機能）】\n「通学する」という機能に焦点があると冠詞なし。\n\n【本問】\ngo to school（特定の建物ではない）。",
      "sentence": "I have to go to (   ) tomorrow.",
      "jp": "明日、学校へ行かなければなりません。",
      "jpPartial": "学校へ"
    },
    {
      "wordNo": 108,
      "word": "car",
      "meaning": "Point C-15: 手段としての交通は無冠詞",
      "detail": "【by car（手段）】\n交通手段を手段として言うとき by＋無冠詞の名詞。\n\n【本問】\nby car。",
      "sentence": "We went there by (   ).",
      "jp": "私たちはそこへ車で行きました。",
      "jpPartial": "車で"
    }
  ]
};

  
  // ====== 状態 ======
  let currentUnit = [];
  let currentIndex = 0;
  let wrongWords = [];
  let isRandom = false;
  /** "word" | "full" | "partialJp" */
  let inputMode = "word";
  let consecutiveCorrect = 0;
  let inputStartTime = null;
  let animSerial = 0;
  let animMode = "simple";
  /** 音声: true のとき次は単語、false のとき次は例文（交互） */
  let speechNextIsWord = true;

  // ====== DOM ======
  const home = document.getElementById("home");
  const quiz = document.getElementById("quiz");
  const resultScreen = document.getElementById("resultScreen");
  const themeToggle = document.getElementById("themeToggle");
  
  const sentenceEl = document.getElementById("sentence");
  const jpEl = document.getElementById("jp");
  const input = document.getElementById("answerInput");
  const resultEl = document.getElementById("result");
  const progress = document.getElementById("progress");
  
  const judgeBtn = document.getElementById("judgeBtn");
  const nextBtn = document.getElementById("nextBtn");
  const speakBtn = document.getElementById("speakBtn");
  const reviewBtn = document.getElementById("reviewBtn");
  const backHomeBtn = document.getElementById("backHome");
  const meaningEl = document.getElementById("meaning");
  const rangeStartInput = document.getElementById("rangeStart");
  const rangeEndInput = document.getElementById("rangeEnd");
  const rangeTestBtn = document.getElementById("rangeTestBtn");
  const appModal = document.getElementById("appModal");
  const appModalBackdrop = document.getElementById("appModalBackdrop");
  const appModalTitle = document.getElementById("appModalTitle");
  const appModalMessage = document.getElementById("appModalMessage");
  const appModalCancel = document.getElementById("appModalCancel");
  const appModalOk = document.getElementById("appModalOk");


  // ====== Word Animation DOM ======
  const animModeEl = document.getElementById("animMode");
  const matchRateEl = document.getElementById("matchRate");
  const wordStageEl = document.getElementById("wordStage");
  const wordSilhouetteEl = document.getElementById("wordSilhouette");
  const wordRippleEl = document.getElementById("wordRipple");
  const wordFallingEl = document.getElementById("wordFalling");
  const wordBreakLayerEl = document.getElementById("wordBreakLayer");
  const quizCardEl = document.querySelector("#quiz .card");
  const globalFxEl = document.getElementById("globalFx");
  const comboDisplayEl = document.getElementById("comboDisplay");
  let appModalResolve = null;
  let appModalIsConfirm = false;

  function closeAppModal(result) {
    if (!appModal || !appModalResolve) return;
    appModal.classList.add("hidden");
    const resolve = appModalResolve;
    appModalResolve = null;
    appModalIsConfirm = false;
    resolve(result);
  }

  function openAppModal({
    title = "お知らせ",
    message = "",
    okText = "はい",
    cancelText = "キャンセル",
    isConfirm = false
  }) {
    if (!appModal || !appModalTitle || !appModalMessage || !appModalOk || !appModalCancel) {
      return Promise.resolve(isConfirm ? false : true);
    }

    appModalTitle.textContent = title;
    appModalMessage.textContent = message;
    appModalOk.textContent = okText;
    appModalCancel.textContent = cancelText;
    appModalCancel.classList.toggle("hidden", !isConfirm);
    appModal.classList.remove("hidden");
    appModalIsConfirm = isConfirm;

    return new Promise(resolve => {
      appModalResolve = resolve;
      setTimeout(() => appModalOk.focus(), 0);
    });
  }

  function showAppAlert(message, title = "お知らせ") {
    return openAppModal({ title, message, isConfirm: false, okText: "はい" });
  }

  function showAppConfirm(message, title = "確認") {
    return openAppModal({
      title,
      message,
      isConfirm: true,
      okText: "はい",
      cancelText: "いいえ"
    });
  }

  appModalOk?.addEventListener("click", () => closeAppModal(true));
  appModalCancel?.addEventListener("click", () => closeAppModal(false));
  appModalBackdrop?.addEventListener("click", () => {
    if (!appModalIsConfirm) closeAppModal(true);
  });
  document.addEventListener(
    "keydown",
    e => {
      if (!appModal || appModal.classList.contains("hidden")) return;
      if (e.key === "Escape") {
        e.preventDefault();
        e.stopPropagation();
        closeAppModal(appModalIsConfirm ? false : true);
        return;
      }
      if (e.key === "Enter" || e.key === "NumpadEnter") {
        e.preventDefault();
        e.stopPropagation();
        closeAppModal(true);
      }
    },
    true
  );


  
  // ====== Unit一覧生成 ======
  const unitList = document.getElementById("unitList");
  Object.keys(units).forEach(key => {
    const btn = document.createElement("button");
    btn.textContent = key.toUpperCase();
    btn.className = "unit-btn";
    btn.onclick = () => startUnit(key);
    unitList.appendChild(btn);
  });
  
  // ====== 設定 ======
  const randomToggleEl = document.getElementById("randomToggle");
  const savedRandom = localStorage.getItem("quizRandom") === "1";
  isRandom = savedRandom;
  if (randomToggleEl) randomToggleEl.checked = savedRandom;

  if (randomToggleEl) {
    randomToggleEl.onchange = e => {
      isRandom = e.target.checked;
      localStorage.setItem("quizRandom", isRandom ? "1" : "0");
    };
  }

  const inputModeSelectEl = document.getElementById("inputModeSelect");
  const savedMode = localStorage.getItem("grammarInputMode");
  if (savedMode === "word" || savedMode === "full" || savedMode === "partialJp") {
    inputMode = savedMode;
  } else {
    inputMode = "word";
  }
  if (inputModeSelectEl) inputModeSelectEl.value = inputMode;

  if (inputModeSelectEl) {
    inputModeSelectEl.addEventListener("change", e => {
      const v = e.target.value;
      inputMode = v === "full" || v === "partialJp" ? v : "word";
      localStorage.setItem("grammarInputMode", inputMode);
    });
  }

  const persistRangeInputs = () => {
    if (rangeStartInput) localStorage.setItem("quizRangeStart", rangeStartInput.value);
    if (rangeEndInput) localStorage.setItem("quizRangeEnd", rangeEndInput.value);
  };

  if (rangeStartInput) {
    const s = localStorage.getItem("quizRangeStart");
    if (s !== null) rangeStartInput.value = s;
    rangeStartInput.addEventListener("change", persistRangeInputs);
    rangeStartInput.addEventListener("input", persistRangeInputs);
    rangeStartInput.addEventListener("blur", persistRangeInputs);
  }
  if (rangeEndInput) {
    const s = localStorage.getItem("quizRangeEnd");
    if (s !== null) rangeEndInput.value = s;
    rangeEndInput.addEventListener("change", persistRangeInputs);
    rangeEndInput.addEventListener("input", persistRangeInputs);
    rangeEndInput.addEventListener("blur", persistRangeInputs);
  }

  // アニメ演出モード
  if (animModeEl) {
    animMode = localStorage.getItem("animMode") || "simple";
    animModeEl.value = animMode;
    animModeEl.onchange = e => {
      animMode = e.target.value;
      localStorage.setItem("animMode", animMode);
    };
  }

  // テーマ切り替え
  if (themeToggle) {
    const moonIconPath = "images/月のアイコン.png";
    const setThemeIcon = isDark => {
      if (isDark) {
        themeToggle.textContent = "☀";
        return;
      }
      themeToggle.innerHTML = `<img src="${moonIconPath}" alt="月アイコン" class="theme-icon">`;
    };

    const savedTheme = localStorage.getItem("theme") || "light";
    if (savedTheme === "dark") {
      document.body.classList.add("dark");
    }
    setThemeIcon(savedTheme === "dark");

    themeToggle.onclick = () => {
      const isDark = document.body.classList.toggle("dark");
      setThemeIcon(isDark);
      localStorage.setItem("theme", isDark ? "dark" : "light");
    };
  }
  
  const homeBtn = document.getElementById("homeBtn");

  homeBtn.style.display = "none";

homeBtn.onclick = async () => {
  const shouldGoHome = await showAppConfirm("Unitを中断してホームに戻りますか？", "確認");
  if (shouldGoHome) {
    quiz.classList.add("hidden");
    resultScreen.classList.add("hidden");
    home.classList.remove("hidden");
    document.body.classList.remove("mode-full-sentence", "mode-partial-jp");

    currentUnit = [];
    currentIndex = 0;
    wrongWords = [];

    homeBtn.style.display = "none";
  }
};
  
  // ====== 開始 ======
  function startUnit(key) {
    const unitWords = units[key].map(entry => ({ ...entry }));
    startQuizWithWords(unitWords);
  }

  function startQuizWithWords(words) {
    currentUnit = [...words];
    if (isRandom) shuffle(currentUnit);
  
    currentIndex = 0;
    wrongWords = [];
    consecutiveCorrect = 0;
    inputStartTime = null;
  
    home.classList.add("hidden");
    quiz.classList.remove("hidden");
  
    homeBtn.style.display = "block";

    showCard();
  }

  // ====== 表示 ======
  function showCard() {
    const q = currentUnit[currentIndex];

    speechNextIsWord = true;

    document.body.classList.remove("mode-full-sentence", "mode-partial-jp");
    if (inputMode === "full") document.body.classList.add("mode-full-sentence");
    if (inputMode === "partialJp") document.body.classList.add("mode-partial-jp");

    if (inputMode === "full") {
      sentenceEl.textContent = q.jp;
      sentenceEl.style.whiteSpace = "pre-wrap";
      jpEl.textContent =
        "この和訳に合う英文を、全文で入力してください。文末のピリオドの有無は問いません。";
      jpEl.style.whiteSpace = "normal";
      input.rows = 4;
      input.placeholder = "英文を全文で入力…";
    } else if (inputMode === "partialJp") {
      sentenceEl.textContent = q.sentence;
      sentenceEl.style.whiteSpace = "";
      jpEl.textContent = `括弧 (   ) に対応する部分の日本語訳を入力してください。\n\n【全体の和訳（参考）】\n${q.jp}`;
      jpEl.style.whiteSpace = "pre-wrap";
      input.rows = 3;
      input.placeholder = "日本語の訳を入力…";
    } else {
      sentenceEl.textContent = q.sentence;
      sentenceEl.style.whiteSpace = "";
      jpEl.textContent = q.jp;
      jpEl.style.whiteSpace = "";
      input.rows = 2;
      input.placeholder = "";
    }
  
    input.value = "";
    input.disabled = false;
    judgeBtn.disabled = false;
    inputStartTime = null;
  
    resultEl.textContent = "";
    resultEl.className = "";
  
    meaningEl.innerHTML = "";
    meaningEl.classList.add("hidden");
  
    matchRateEl?.classList.add("hidden");

    // レイヤー状態リセット
    wordStageEl?.classList.add("hidden");
    wordStageEl?.classList.remove("state-correct", "state-incorrect");
    wordFallingEl?.classList.remove("anim-correct", "anim-wrong-shift", "anim-wrong-rotate", "anim-wrong-drift", "anim-wrong-ugly");
    wordRippleEl?.classList.remove("play");
    if (wordBreakLayerEl) {
      wordBreakLayerEl.innerHTML = "";
      wordBreakLayerEl.classList.remove("play");
    }

    // コンボ表示リセット
    if (comboDisplayEl) {
      comboDisplayEl.classList.add("hidden");
      comboDisplayEl.classList.remove("tier-1", "tier-2", "play");
      comboDisplayEl.textContent = "";
    }

    // 全画面FX状態リセット
    document.body.classList.remove(
      "scene-correct",
      "scene-incorrect",
      "scene-hold",
      "scene-audio-pulse",
      "fx-combo-1",
      "fx-combo-2"
    );
    if (globalFxEl) {
      globalFxEl.classList.add("hidden");
      globalFxEl.classList.remove("scene-hold");
      const particlesWrap = globalFxEl.querySelector(".fx-particles");
      if (particlesWrap) particlesWrap.innerHTML = "";
    }

    nextBtn.classList.add("hidden");
    speakBtn.classList.add("hidden");
  
    const numberLabel = q.number ? `No.${q.number} ` : "";
    const modeTag =
      inputMode === "full" ? "全文 " : inputMode === "partialJp" ? "部分訳 " : "";
    progress.textContent = `${modeTag}${numberLabel}${currentIndex + 1} / ${currentUnit.length}`;
    input.focus();
  }
  
  // ====== Word Animation helpers ======
  function normalizeWord(str) {
    return (str || "")
      .trim()
      .toLowerCase()
      .replace(/\s+/g, " ");
  }

  // 文字位置ベースの一致率（完全一致なら100%）
  function calcMatchRate(userStr, targetStr) {
    const u = normalizeWord(userStr);
    const t = normalizeWord(targetStr);

    const maxLen = Math.max(u.length, t.length);
    if (maxLen === 0) return 100;

    const minLen = Math.min(u.length, t.length);
    let match = 0;
    for (let i = 0; i < minLen; i++) {
      if (u[i] === t[i]) match++;
    }

    return Math.max(0, Math.min(100, Math.round((match / maxLen) * 100)));
  }

  /** 日本語など大小文字を変えない一致率 */
  function calcMatchRateChars(userStr, targetStr) {
    const u = userStr || "";
    const t = targetStr || "";
    const maxLen = Math.max(u.length, t.length);
    if (maxLen === 0) return 100;
    const minLen = Math.min(u.length, t.length);
    let match = 0;
    for (let i = 0; i < minLen; i++) {
      if (u[i] === t[i]) match++;
    }
    return Math.max(0, Math.min(100, Math.round((match / maxLen) * 100)));
  }

  function expectedFullSentence(q) {
    return (q.sentence || "").replace(/\(\s+\)/g, (q.word || "").trim());
  }

  /** 全文モード比較用：末尾句読点の有無などを吸収 */
  function normalizeSentenceAnswer(s) {
    let t = normalizeWord(s);
    t = t.replace(/\u2019/g, "'").replace(/\u2018/g, "'").replace(/\u201c/g, '"').replace(/\u201d/g, '"');
    while (/[.,!?;:…]+$/u.test(t)) {
      t = t.replace(/[.,!?;:…]+$/u, "").trim();
    }
    return t;
  }

  function normalizeJp(s) {
    let t = (s || "").normalize("NFKC").trim().replace(/\s+/g, "");
    t = t.replace(/\u3000/g, "");
    while (/[。．、，!?！？]+$/.test(t)) {
      t = t.replace(/[。．、，!?！？]+$/g, "");
    }
    return t;
  }

  function clipAnimText(text, maxLen) {
    const s = (text || "").trim();
    const shouldClip = inputMode === "full" || inputMode === "partialJp";
    if (!shouldClip || s.length <= maxLen) return s;
    return `${s.slice(0, Math.max(0, maxLen - 1))}…`;
  }

  function getComboTier(count) {
    if (count >= 5) return 2;
    if (count >= 3) return 1;
    return 0;
  }

  function lerp(a, b, t) {
    return a + (b - a) * t;
  }

  function randBetween(min, max) {
    return Math.random() * (max - min) + min;
  }

  // --- Audio (WebAudio) ---
  let audioCtx = null;
  function ensureAudioCtx() {
    if (audioCtx) return audioCtx;
    const Ctx = window.AudioContext || window.webkitAudioContext;
    if (!Ctx) return null;
    try {
      audioCtx = new Ctx();
    } catch {
      return null;
    }
    return audioCtx;
  }

  // スマホ対策：AudioContextはユーザー操作直後にresumeしないと無音になりがち
  function primeAudio() {
    const ctx = ensureAudioCtx();
    if (!ctx) return;
    try {
      if (ctx.state === "suspended") {
        ctx.resume().catch(() => {});
      }
    } catch {
      // ignore
    }
  }

  function playTone(freq, durationMs, type = "sine", gainValue = 0.03) {
    const ctx = ensureAudioCtx();
    if (!ctx) return;
    try {
      if (ctx.state === "suspended") ctx.resume().catch(() => {});
      const osc = ctx.createOscillator();
      const gain = ctx.createGain();
      osc.type = type;
      osc.frequency.value = freq;

      const now = ctx.currentTime;
      gain.gain.setValueAtTime(0.0001, now);
      gain.gain.exponentialRampToValueAtTime(Math.max(0.0002, gainValue), now + 0.01);
      gain.gain.exponentialRampToValueAtTime(0.0001, now + durationMs / 1000);

      osc.connect(gain);
      gain.connect(ctx.destination);
      osc.start(now);
      osc.stop(now + durationMs / 1000 + 0.02);
    } catch {
      // 音が鳴らなくても学習体験は止めない
    }
  }

  let correct2AudioEl = null;
  function playCorrect2Sound() {
    primeAudio();
    try {
      if (!correct2AudioEl) {
        correct2AudioEl = new Audio("クイズ正解2.mp3");
        correct2AudioEl.preload = "auto";
      }
      correct2AudioEl.pause();
      correct2AudioEl.currentTime = 0;
      void correct2AudioEl.play().catch(() => {});
    } catch {
      // ignore
    }
  }

  function playJudgeSound(kind) {
    // kind: fall / click / wrong / correct2（correct2 はクイズ正解2.mp3）
    // 正解時：刺さりにくい（低め・サイン寄り・音量控えめ）
    if (kind === "fallCorrect") playTone(560, 45, "sine", 0.012);
    if (kind === "fallWrong") playTone(190, 65, "triangle", 0.012);
    if (kind === "fall") playTone(520, 55, "sine", 0.012);

    // 正解時クリック：高音域より「ふわっとした短い音」（2音）
    if (kind === "click") {
      playTone(880, 75, "sine", 0.018);
      setTimeout(() => playTone(1320, 55, "sine", 0.009), 12);
    }
    if (kind === "correct2") playCorrect2Sound();
    if (kind === "wrong") playTone(180, 140, "triangle", 0.018);
  }

  function scheduleFallingRhythm(serial, fallDurationMs, isExact) {
    if (!fallDurationMs || fallDurationMs < 1) return;
    const beats = isExact ? 3 : 4;
    const baseAt = Math.round(fallDurationMs * 0.22);
    for (let i = 0; i < beats; i++) {
      const t = baseAt + Math.round((fallDurationMs * 0.58) * (i / (beats - 1)));
      setTimeout(() => {
        if (serial !== animSerial) return;
        playJudgeSound(isExact ? "fallCorrect" : "fallWrong");
        triggerAudioPulse();
      }, t);
    }
  }

  function createBreakdown(userDisplay, matchRate) {
    if (!wordBreakLayerEl) return;
    wordBreakLayerEl.innerHTML = "";

    const text = normalizeWord(userDisplay);
    const chars = text.split("");
    const mismatch = 1 - matchRate / 100;

    // mismatchが大きいほど飛び散る（違和感増幅）
    const scatterX = 10 + mismatch * 18;
    const scatterY = 20 + mismatch * 35;
    const scatterRot = 45 + mismatch * 120;
    const baseDelay = 60 + mismatch * 90;

    chars.forEach(ch => {
      const span = document.createElement("span");
      span.className = "break-char" + (ch === " " ? " space" : "");
      span.textContent = ch === " " ? " " : ch;

      const dx = randBetween(-scatterX, scatterX);
      const dy = randBetween(-scatterY * 0.9, -scatterY * 0.2);
      const dr = randBetween(-scatterRot, scatterRot);
      const delay = Math.round(Math.random() * baseDelay);

      span.style.setProperty("--dx", `${dx.toFixed(1)}px`);
      span.style.setProperty("--dy", `${dy.toFixed(1)}px`);
      span.style.setProperty("--dr", `${dr.toFixed(1)}deg`);
      span.style.setProperty("--delay", `${delay}ms`);

      wordBreakLayerEl.appendChild(span);
    });
  }

  function runWordAnimation({
    userDisplay,
    userNorm,
    correctDisplay,
    correctNorm,
    isExact,
    matchRate,
    comboTier,
    elapsedMs
  }) {
    const serial = ++animSerial;

    if (!wordStageEl || !wordFallingEl || !wordSilhouetteEl) return 700;

    // Stage位置合わせ（カード内で入力位置と重ねる）
    if (quizCardEl) {
      const cardRect = quizCardEl.getBoundingClientRect();
      const inputRect = input.getBoundingClientRect();
      const stageY = (inputRect.top - cardRect.top) + inputRect.height * 0.55;
      wordStageEl.style.top = `${stageY}px`;
    }

    // mode + combo
    wordStageEl.classList.remove("anim-mode-simple", "anim-mode-neon", "anim-mode-pop");
    wordStageEl.classList.add(`anim-mode-${animMode}`);

    wordStageEl.classList.remove("combo-tier-0", "combo-tier-1", "combo-tier-2");
    wordStageEl.classList.add(`combo-tier-${comboTier}`);

    // コンボ上位は色で気持ちよさを強化
    if (comboTier >= 2) {
      if (animMode === "simple") wordStageEl.style.setProperty("--glow", "rgba(124, 58, 237, 0.95)");
      if (animMode === "neon") wordStageEl.style.setProperty("--glow", "rgba(217, 70, 239, 0.95)");
      if (animMode === "pop") wordStageEl.style.setProperty("--glow", "rgba(249, 115, 22, 0.95)");
      wordStageEl.style.setProperty("--rippleBoost", "1.55");
    } else if (comboTier === 1) {
      wordStageEl.style.setProperty("--rippleBoost", "1.25");
    }

    // speed連動（速いほど短く・ズレにくく）
    const elapsedClamped = Math.max(0, Math.min(2500, elapsedMs || 0));
    const fastness = Math.max(0, Math.min(1, 1 - elapsedClamped / 2500)); // 1=超速い
    const fallDuration = Math.round(lerp(900, 560, fastness));

    wordStageEl.style.setProperty("--fallDuration", `${fallDuration}ms`);
    wordStageEl.style.setProperty("--startY", `${Math.round(-140 - (1 - fastness) * 20)}px`);

    // 影の濃さ（不正解ほど徐々に濃く、かつ一致率が低いほど濃く）
    const mismatch = 1 - matchRate / 100;
    const silhEnd = Math.min(0.9, 0.58 + mismatch * 0.36);
    wordStageEl.style.setProperty("--silhEndOpacity", silhEnd.toFixed(2));

    // テキスト更新（全文モードは落下アニメ用に短く切り詰め）
    wordSilhouetteEl.textContent = clipAnimText(correctDisplay, 40);
    wordFallingEl.textContent = clipAnimText(
      isExact ? correctDisplay : userDisplay.trim().length > 0 ? userDisplay : correctDisplay,
      40
    );
    wordRippleEl?.classList.remove("play");
    if (wordBreakLayerEl) {
      wordBreakLayerEl.innerHTML = "";
      wordBreakLayerEl.classList.remove("play");
    }

    // 初期状態クラス
    wordStageEl.classList.remove("state-correct", "state-incorrect");
    wordFallingEl.classList.remove("anim-correct", "anim-wrong-shift", "anim-wrong-rotate", "anim-wrong-drift", "anim-wrong-ugly");

    // --- 正解 ---
    if (isExact) {
      wordStageEl.classList.add("state-correct");
      wordFallingEl.classList.add("anim-correct");

      wordStageEl.classList.remove("state-incorrect");

      // リズム音（落下中）
      scheduleFallingRhythm(serial, fallDuration, true);

      // 波紋（余韻）
      const rippleAt = Math.round(fallDuration * 0.78);
      setTimeout(() => {
        if (serial !== animSerial) return;
        if (!wordRippleEl) return;
        wordRippleEl.classList.remove("play");
        void wordRippleEl.offsetWidth; // 再生を確実にする
        wordRippleEl.classList.add("play");
        // 正解音2は judge 側で読み上げに合わせて再生
      }, rippleAt);

      // 表示開始
      wordStageEl.classList.remove("hidden");
      return fallDuration;
    }

    // --- 不正解 ---
    wordStageEl.classList.add("state-incorrect");

    // ランダム演出（少しズレ / 回転 / 横にズレ続ける）
    // 不一致が大きいほど「歪み拒否（ugly）」に寄せる
    const variants = ["shift", "rotate", "drift", "ugly"];
    let variant = variants[Math.floor(Math.random() * variants.length)];
    if (mismatch >= 0.18 && Math.random() < 0.55 + mismatch * 0.25) variant = "ugly";
    if (matchRate < 75 && Math.random() < 0.75) variant = "ugly";

    // 速いほどズレを減らす（ぴったり一致しやすく）
    const driftFactor = 0.85 + (1 - fastness) * 0.55;
    const driftBase = (mismatch * 20 + 6) * driftFactor;

    const endX = randBetween(-driftBase, driftBase);
    const endRot = randBetween(-driftBase * 0.12, driftBase * 0.12);
    const driftX = randBetween(-driftBase, driftBase);

    // "気持ち悪い"ほど、ズレを強める（歪み拒否）
    if (variant === "ugly") {
      const uglyEndX = endX * randBetween(1.25, 1.9);
      const uglyEndRot = endRot * randBetween(1.35, 2.1);
      const jitterX = randBetween(-Math.abs(uglyEndX) * 0.35, Math.abs(uglyEndX) * 0.35);
      const jitterY = randBetween(-14 - mismatch * 22, -4 - mismatch * 10);
      const jitterRot = randBetween(-Math.abs(uglyEndRot) * 0.75, Math.abs(uglyEndRot) * 0.75);

      wordStageEl.style.setProperty("--startY", `${Math.round(-190 - (1 - fastness) * 30)}px`);
      wordStageEl.style.setProperty("--endX", `${uglyEndX.toFixed(1)}px`);
      wordStageEl.style.setProperty("--endRot", `${uglyEndRot.toFixed(2)}deg`);
      wordStageEl.style.setProperty("--jitterX", `${jitterX.toFixed(1)}px`);
      wordStageEl.style.setProperty("--jitterY", `${jitterY.toFixed(1)}px`);
      wordStageEl.style.setProperty("--jitterRot", `${jitterRot.toFixed(2)}deg`);

      wordFallingEl.classList.add("anim-wrong-ugly");
    } else if (variant === "shift") {
      wordStageEl.style.setProperty("--endX", `${endX.toFixed(1)}px`);
      wordStageEl.style.setProperty("--endRot", `${endRot.toFixed(2)}deg`);
      wordFallingEl.classList.add("anim-wrong-shift");
    } else if (variant === "rotate") {
      wordStageEl.style.setProperty("--endX", `${endX.toFixed(1)}px`);
      wordStageEl.style.setProperty("--endRot", `${endRot.toFixed(2)}deg`);
      wordFallingEl.classList.add("anim-wrong-rotate");
    } else {
      wordStageEl.style.setProperty("--endX", `0px`);
      wordStageEl.style.setProperty("--endRot", `${endRot.toFixed(2)}deg`);
      wordStageEl.style.setProperty("--driftX", `${driftX.toFixed(1)}px`);
      wordFallingEl.classList.add("anim-wrong-drift");
    }

    // （不正解）落下中のリズム音を鈍く
    // ※ズレ/歪み演出は別で強制されるので、ここは気持ち悪さの質感に寄せる
    scheduleFallingRhythm(serial, fallDuration, false);

    // 入力単語の崩壊（分解して消える）
    const breakAt = Math.round(fallDuration * 0.64);
    setTimeout(() => {
      if (serial !== animSerial) return;
      if (!wordBreakLayerEl) return;
      createBreakdown(clipAnimText(userDisplay || "", 120) || "", matchRate);
      wordBreakLayerEl.classList.remove("play");
      void wordBreakLayerEl.offsetWidth;
      wordBreakLayerEl.classList.add("play");
      playJudgeSound("wrong");
    }, breakAt);

    wordStageEl.classList.remove("hidden");
    return fallDuration;
  }

  // ====== Global scene FX helpers ======
  let sceneSerial = 0;
  let audioPulseSerial = 0;

  function setComboClass(comboTier) {
    document.body.classList.remove("fx-combo-1", "fx-combo-2");
    if (comboTier >= 2) document.body.classList.add("fx-combo-2");
    else if (comboTier === 1) document.body.classList.add("fx-combo-1");
  }

  function showGlobalFx() {
    if (!globalFxEl) return;
    globalFxEl.classList.remove("hidden");
  }

  function hideGlobalFxLater(serial, ms) {
    if (!globalFxEl) return;
    setTimeout(() => {
      if (serial !== sceneSerial) return;
      globalFxEl.classList.add("hidden");
      const particlesWrap = globalFxEl.querySelector(".fx-particles");
      if (particlesWrap) particlesWrap.innerHTML = "";
    }, ms);
  }

  function triggerAudioPulse() {
    const body = document.body;
    const serial = ++audioPulseSerial;
    body.classList.remove("scene-audio-pulse");
    // 小さな揺れは毎回付け直す
    body.classList.add("scene-audio-pulse");
    setTimeout(() => {
      if (serial !== audioPulseSerial) return;
      body.classList.remove("scene-audio-pulse");
    }, 90);
  }

  function createParticles(comboTier) {
    if (!globalFxEl) return;
    const wrap = globalFxEl.querySelector(".fx-particles");
    if (!wrap) return;
    wrap.innerHTML = "";

    const count = comboTier >= 2 ? 52 : (comboTier === 1 ? 36 : 24);
    const spread = comboTier >= 2 ? 340 : (comboTier === 1 ? 280 : 220);
    const centerX = window.innerWidth * 0.5;
    const centerY = window.innerHeight * 0.42;

    for (let i = 0; i < count; i++) {
      const p = document.createElement("span");
      p.className = "p";

      // start/end: center -> random direction
      const a = Math.random() * Math.PI * 2;
      const r = Math.pow(Math.random(), 0.7) * spread;
      const ex = Math.cos(a) * r;
      const ey = Math.sin(a) * r;

      // ほぼ中心開始
      p.style.left = `${centerX}px`;
      p.style.top = `${centerY}px`;
      p.style.setProperty("--sx", "0px");
      p.style.setProperty("--sy", "0px");
      p.style.setProperty("--ex", `${ex.toFixed(1)}px`);
      p.style.setProperty("--ey", `${ey.toFixed(1)}px`);

      const delay = Math.round(Math.random() * 90);
      const dur = Math.round(650 + Math.random() * 220);
      p.style.setProperty("--ptDelay", `${delay}ms`);
      p.style.setProperty("--ptDur", `${dur}ms`);

      if (comboTier >= 1) {
        // コンボほど色を増やす
        const hue = comboTier >= 2 ? 265 : 206;
        const sat = comboTier >= 2 ? 95 : 85;
        p.style.background = `hsla(${hue}, ${sat}%, 75%, 0.95)`;
        p.style.boxShadow = `0 0 18px hsla(${hue}, ${sat}%, 55%, 0.45)`;
      }

      wrap.appendChild(p);
    }
  }

  function triggerScene(isExact, comboTier) {
    sceneSerial++;
    const serial = sceneSerial;

    document.body.classList.remove(
      "scene-correct",
      "scene-incorrect",
      "scene-hold",
      "scene-audio-pulse",
      "fx-combo-1",
      "fx-combo-2"
    );

    document.body.classList.add(isExact ? "scene-correct" : "scene-incorrect");
    setComboClass(comboTier);

    if (globalFxEl) {
      showGlobalFx();
      if (isExact) {
        createParticles(comboTier);
      }
    }

    // 不正解の場合はユーザーが「次へ」を押すまで待つので、ぼかし等が残り続けないように一定時間で解除
    const cleanupMs = isExact ? 1100 : 720;
    setTimeout(() => {
      if (serial !== sceneSerial) return;
      document.body.classList.remove(
        "scene-correct",
        "scene-incorrect",
        "scene-hold",
        "scene-audio-pulse",
        "fx-combo-1",
        "fx-combo-2"
      );
    }, cleanupMs);

    // 余韻で隠す
    hideGlobalFxLater(serial, isExact ? 1200 : 800);
  }

  function showComboText(consecutiveCount, comboTier) {
    if (!comboDisplayEl) return;

    if (comboTier < 1 || consecutiveCount < 3) {
      comboDisplayEl.classList.add("hidden");
      comboDisplayEl.classList.remove("tier-1", "tier-2", "play");
      return;
    }

    comboDisplayEl.textContent = `COMBO x${consecutiveCount}`;
    comboDisplayEl.classList.remove("hidden", "tier-1", "tier-2", "play");
    comboDisplayEl.classList.add(`tier-${comboTier}`);

    // アニメ再生を確実にする
    void comboDisplayEl.offsetWidth;
    comboDisplayEl.classList.add("play");
  }

  function setGrammarPointsPanel(q) {
    meaningEl.classList.remove("hidden");
    meaningEl.innerHTML = "";
    const wrap = document.createElement("div");
    wrap.className = "grammar-meaning";

    const head = document.createElement("div");
    head.className = "grammar-meaning-head";
    head.textContent = "文法ポイント";

    const fullAns = expectedFullSentence(q);

    const ans = document.createElement("div");
    ans.className = "grammar-meaning-answer";
    if (inputMode === "full") {
      ans.textContent = `模範解答（全文）：${fullAns}`;
    } else if (inputMode === "partialJp") {
      const jpP = (q.jpPartial || "").trim();
      ans.textContent = jpP ? `模範（括弧の和訳）：${jpP}` : `正解：${q.word}`;
    } else {
      ans.textContent = `正解：${q.word}`;
    }

    const sum = document.createElement("div");
    sum.className = "grammar-meaning-summary";
    sum.textContent = q.meaning || "";

    wrap.appendChild(head);
    wrap.appendChild(ans);
    wrap.appendChild(sum);

    const det = (q.detail || "").trim();
    if (det) {
      const pre = document.createElement("pre");
      pre.className = "grammar-meaning-detail";
      pre.textContent = det;
      wrap.appendChild(pre);
    }

    meaningEl.appendChild(wrap);
  }

  // ====== 判定 ======
  function judge() {
    if (input.disabled) return;
  
    // ユーザー操作（判定ボタン/Enter）直後にSE用AudioContextを起こす
    primeAudio();

    const q = currentUnit[currentIndex];
    const userDisplay = input.value.trim();
    const expectedFull = expectedFullSentence(q);

    let userNorm;
    let correctDisplay;
    let correctNorm;
    let isExact;
    let matchRate;

    if (inputMode === "full") {
      userNorm = normalizeSentenceAnswer(userDisplay);
      correctDisplay = expectedFull;
      correctNorm = normalizeSentenceAnswer(expectedFull);
      isExact = userNorm === correctNorm && userNorm.length > 0;
      matchRate = calcMatchRate(userNorm, correctNorm);
    } else if (inputMode === "partialJp") {
      const jpRef = (q.jpPartial || "").trim();
      userNorm = normalizeJp(userDisplay);
      correctDisplay = jpRef || q.jp;
      correctNorm = normalizeJp(jpRef);
      isExact = jpRef.length > 0 && userNorm === correctNorm;
      matchRate = calcMatchRateChars(userNorm, correctNorm);
    } else {
      userNorm = normalizeWord(userDisplay);
      correctDisplay = q.word;
      correctNorm = normalizeWord(correctDisplay);
      isExact = userNorm === correctNorm;
      matchRate = calcMatchRate(userNorm, correctNorm);
    }
    const nextComboCount = isExact ? consecutiveCorrect + 1 : 0;
    const comboTier = getComboTier(nextComboCount);
    const elapsedMs = inputStartTime ? (performance.now() - inputStartTime) : 0;
  
    input.disabled = true;
    judgeBtn.disabled = true;
  
    // 文法ポイント（要約＋詳細）
    setGrammarPointsPanel(q);
  
    matchRateEl && (matchRateEl.textContent = `${matchRate}% match`);
    matchRateEl?.classList.remove("hidden");

    // アニメ中は誤操作防止
    speakBtn.disabled = true;
    nextBtn.disabled = true;
    speakBtn.classList.add("hidden");
    nextBtn.classList.add("hidden");

    // ====== 全画面シーン演出 ======
    triggerScene(isExact, comboTier);
    if (isExact) {
      if (globalFxEl) {
        globalFxEl.classList.add("scene-hold");
        setTimeout(() => globalFxEl.classList.remove("scene-hold"), 120);
      }
    }

    const fallDuration = runWordAnimation({
      userDisplay,
      userNorm,
      correctDisplay,
      correctNorm,
      isExact,
      matchRate,
      comboTier,
      elapsedMs
    });

    if (isExact) {
      consecutiveCorrect = nextComboCount;
      resultEl.textContent = "⭕ 正解";
      resultEl.className = "correct";

      // コンボ表示（3連続以上）
      showComboText(nextComboCount, comboTier);

      // 正解音2 → わずかに先行してから読み上げ（同時感を出しつつ先に入る）
      playJudgeSound("correct2");
      setTimeout(() => {
        const toSpeak =
          inputMode === "full"
            ? sentenceForSpeech(q)
            : inputMode === "partialJp"
              ? (q.jpPartial || "").trim() || q.word
              : q.word;
        speakWord(toSpeak);
        speechNextIsWord = false;
      }, 36);

      setTimeout(() => {
        next();
      }, fallDuration + 120);
    } else {
      consecutiveCorrect = 0;
      if (comboDisplayEl) {
        comboDisplayEl.classList.add("hidden");
        comboDisplayEl.classList.remove("play");
      }
      resultEl.textContent =
        inputMode === "full"
          ? `❌ 不正解（模範全文は下のポイント欄）`
          : inputMode === "partialJp"
            ? `❌ 不正解（模範の和訳は下のポイント欄）`
            : `❌ 不正解：${q.word}`;
      resultEl.className = "wrong";
      wrongWords.push(q);

      // 正解と同様、判定直後に読み上げ（落下リズム・不正解SEと時間が重なりやすくなる）
      const toSpeakWrong =
        inputMode === "full"
          ? sentenceForSpeech(q)
          : inputMode === "partialJp"
            ? (q.jpPartial || "").trim() || q.word
            : q.word;
      speakWord(toSpeakWrong);
      speechNextIsWord = false;

      setTimeout(() => {
        if (input.disabled === false) return;
        speakBtn.disabled = false;
        nextBtn.disabled = false;
        speakBtn.classList.remove("hidden");
        nextBtn.classList.remove("hidden");
      }, fallDuration + 40);
    }
  }
  // ====== 次 ======
  function next() {
    currentIndex++;
    if (currentIndex >= currentUnit.length) {
      endUnit();
    } else {
      showCard();
    }
  }
  
  // ====== 終了 ======
  function endUnit() {
    quiz.classList.add("hidden");
    resultScreen.classList.remove("hidden");

    homeBtn.style.display = "none";
  
    if (wrongWords.length > 0) {
      reviewBtn.classList.remove("hidden");
    } else {
      reviewBtn.classList.add("hidden");
    }
  }
  
  // ====== 復習 ======
  reviewBtn.onclick = () => {
    currentUnit = [...wrongWords];
    wrongWords = [];
    currentIndex = 0;
    consecutiveCorrect = 0;
    inputStartTime = null;
  
    resultScreen.classList.add("hidden");
    quiz.classList.remove("hidden");

    homeBtn.style.display = "block";

    showCard();
  };
  
  // ====== ホーム ======
  backHomeBtn.onclick = () => {
    resultScreen.classList.add("hidden");
    home.classList.remove("hidden");
  };
  
// ====== 発音（スマホ対応 改良版） ======
function isLikelyFemaleVoiceName(name) {
  if (!name) return false;
  const n = name.toLowerCase();
  if (n.includes("female")) return true;
  const hints = [
    "zira",
    "jenny",
    "aria",
    "samantha",
    "victoria",
    "karen",
    "tessa",
    "moira",
    "fiona",
    "serena",
    "veena",
    "emma",
    "amy",
    "olivia",
    "sarah",
    "hazel",
    "martha",
    "catherine",
    "google us english female",
    "google uk english female"
  ];
  return hints.some(h => n.includes(h));
}

function resolveEnglishVoice() {
  const voices = window.speechSynthesis.getVoices();
  if (!voices?.length) return null;

  const langNorm = s => (s || "").toLowerCase().replace("_", "-");

  function rankGoogleUsFemale(v) {
    const name = (v.name || "").toLowerCase();
    const lang = langNorm(v.lang);
    if (!name.includes("google")) return -1;
    if (lang !== "en-us" && !lang.startsWith("en-us")) return -1;
    let r = 0;
    if (name.includes("female")) r += 120;
    else if (/wavenet-f|neural2-f|neural-f|[-_]f\b|_f_/i.test(name)) r += 100;
    if (name.includes("journey")) r += 55;
    if (name.includes("generative")) r += 50;
    if (name.includes("premium")) r += 42;
    if (name.includes("neural2")) r += 38;
    if (name.includes("wavenet")) r += 35;
    if (name.includes("neural")) r += 32;
    if (name.includes("studio")) r += 28;
    if (name.includes("polyglot")) r += 22;
    if (name.includes("us english")) r += 18;
    if (name.includes("en-us")) r += 8;
    return r;
  }

  /** 同スコア時は「明るい／高めになりやすい」エンジンを優先 */
  function tieBreakGoogleFemale(a, b) {
    const key = v => {
      const n = (v.name || "").toLowerCase();
      let k = 0;
      if (n.includes("journey")) k += 80;
      if (n.includes("generative")) k += 70;
      if (n.includes("neural2")) k += 60;
      if (n.includes("premium")) k += 50;
      if (n.includes("wavenet")) k += 40;
      if (n.includes("neural")) k += 35;
      if (n.includes("studio")) k += 30;
      if (n.includes("female")) k += 25;
      return k;
    };
    return key(b) - key(a);
  }

  const googleUsFemaleCandidates = voices
    .map(v => ({ v, r: rankGoogleUsFemale(v) }))
    .filter(x => x.r >= 100)
    .sort((a, b) => {
      if (b.r !== a.r) return b.r - a.r;
      return tieBreakGoogleFemale(a.v, b.v);
    });
  if (googleUsFemaleCandidates.length > 0) return googleUsFemaleCandidates[0].v;

  const englishVoices = voices.filter(v => langNorm(v.lang).startsWith("en"));
  if (englishVoices.length === 0) return null;

  const femaleVoices = englishVoices.filter(v => isLikelyFemaleVoiceName(v.name));

  const femalePriority = [
    "Google US English Female",
    "Google UK English Female",
    "Microsoft Jenny",
    "Microsoft Aria",
    "Microsoft Zira",
    "Samantha",
    "Victoria",
    "Karen",
    "Tessa",
    "Moira",
    "Fiona"
  ];
  for (const name of femalePriority) {
    const found = femaleVoices.find(v => v.name.indexOf(name) !== -1);
    if (found) return found;
  }
  if (femaleVoices.length > 0) return femaleVoices[0];

  const softFemaleNames = ["Samantha", "Victoria", "Karen", "Moira", "Tessa", "Fiona", "Serena"];
  for (const name of softFemaleNames) {
    const found = englishVoices.find(v => v.name.indexOf(name) !== -1);
    if (found) return found;
  }

  const googleUs = englishVoices.find(
    v => /google/i.test(v.name) && /us english/i.test(v.name.toLowerCase())
  );
  if (googleUs) return googleUs;

  return englishVoices[0];
}

function speakWord(word) {
  if (!("speechSynthesis" in window)) return;

  const synth = window.speechSynthesis;
  synth.cancel();

  const isJpText = /[\u3040-\u309F\u30A0-\u30FF\u4E00-\u9FFF]/.test(word || "");

  const doSpeak = () => {
    const utter = new SpeechSynthesisUtterance(word);
    utter.lang = isJpText ? "ja-JP" : "en-US";
    utter.rate = isJpText ? 1.0 : 1.05;
    utter.pitch = isJpText ? 1.0 : 1.26;
    if (!isJpText) {
      const v = resolveEnglishVoice();
      if (v) utter.voice = v;
    }
    synth.speak(utter);
  };

  if (isJpText) {
    doSpeak();
    return;
  }

  if (resolveEnglishVoice()) {
    doSpeak();
    return;
  }

  // voiceschanged はブラウザによって複数回飛ぶ。毎回 speak すると二重読み上げになるので1回だけ実行する
  let spoken = false;
  const fireOnce = () => {
    if (spoken) return;
    spoken = true;
    synth.removeEventListener("voiceschanged", onVoicesChanged);
    clearTimeout(fallbackTimer);
    doSpeak();
  };
  const onVoicesChanged = () => fireOnce();
  const fallbackTimer = setTimeout(fireOnce, 800);
  synth.addEventListener("voiceschanged", onVoicesChanged);
  void synth.getVoices();
}

function sentenceForSpeech(q) {
  return (q.sentence || "").replace(/\(\s+\)/g, q.word);
}

function playAlternatingSpeech() {
  if (!("speechSynthesis" in window)) return;
  const q = currentUnit[currentIndex];
  if (!q) return;
  primeAudio();
  const text = speechNextIsWord ? q.word : sentenceForSpeech(q);
  speakWord(text);
  speechNextIsWord = !speechNextIsWord;
}

speakBtn.onclick = () => {
  playAlternatingSpeech();
};
  
  // ====== イベント ======
  judgeBtn.onclick = judge;
  nextBtn.onclick = next;
  
  input.addEventListener("focus", () => {
    if (inputStartTime === null) inputStartTime = performance.now();
  });

  input.addEventListener("input", () => {
    if (inputStartTime === null && input.value.trim().length > 0) inputStartTime = performance.now();
  });

  input.addEventListener("keydown", e => {
    if (e.key !== "Enter") return;
    e.preventDefault();

    if (!input.disabled) {
      judge();
      return;
    }

    const canGoNextAfterJudge =
      !nextBtn.disabled && !nextBtn.classList.contains("hidden");
    if (canGoNextAfterJudge) next();
  });

  // inputがdisabled中はinput自身でkeydownを受け取れないため、画面全体でもEnterを監視
  document.addEventListener("keydown", e => {
    if (e.key !== "Enter") return;
    if (appModal && !appModal.classList.contains("hidden")) return;
    if (quiz.classList.contains("hidden")) return;
    if (!input.disabled) return;

    const canGoNextAfterJudge =
      !nextBtn.disabled && !nextBtn.classList.contains("hidden");
    if (!canGoNextAfterJudge) return;

    e.preventDefault();
    next();
  });

  // V: 判定後のみ。単語と例文を交互に読み上げ（判定前は v を英文に打てるようにする）
  document.addEventListener("keydown", e => {
    if (e.key !== "v" && e.key !== "V") return;
    if (e.repeat || e.ctrlKey || e.metaKey || e.altKey) return;
    if (appModal && !appModal.classList.contains("hidden")) return;
    if (quiz.classList.contains("hidden")) return;
    if (!input.disabled) return;

    e.preventDefault();
    playAlternatingSpeech();
  });

  // Unit終了画面: Enter で「間違えた単語を復習」（表示中なら）／なければホームへ
  document.addEventListener("keydown", e => {
    if (e.key !== "Enter") return;
    if (e.repeat) return;
    if (appModal && !appModal.classList.contains("hidden")) return;
    if (resultScreen.classList.contains("hidden")) return;

    e.preventDefault();
    if (!reviewBtn.classList.contains("hidden")) {
      reviewBtn.click();
    } else {
      backHomeBtn.click();
    }
  });

  // Alt+H: クイズ中はホームボタン（🏠）と同じ／Unit終了画面ならホームへ戻る
  document.addEventListener("keydown", e => {
    if (!e.altKey || e.ctrlKey || e.metaKey) return;
    if (e.key !== "h" && e.key !== "H") return;
    if (e.repeat) return;
    if (appModal && !appModal.classList.contains("hidden")) return;

    const appEl = document.getElementById("app");
    if (!appEl || appEl.style.display === "none") return;

    if (!quiz.classList.contains("hidden") && homeBtn.style.display === "block") {
      e.preventDefault();
      homeBtn.click();
      return;
    }
    if (!resultScreen.classList.contains("hidden")) {
      e.preventDefault();
      backHomeBtn?.click();
    }
  });
  
  // ====== シャッフル ======
  function shuffle(arr) {
    for (let i = arr.length - 1; i > 0; i--) {
      const j = Math.floor(Math.random() * (i + 1));
      [arr[i], arr[j]] = [arr[j], arr[i]];
    }
  }
  function login() {
    const password = document.getElementById("pw").value;
    const correctPassword = ""; // 好きなパスワード
  
    if (password === correctPassword) {
      document.getElementById("login").style.display = "none";
      document.getElementById("app").style.display = "block";
    } else {
      showAppAlert("パスワードが違います", "入力エラー");
    }
  }
  function getAllWords() {
    let all = [];
    Object.values(units).forEach(arr => {
      arr.forEach(entry => {
        all.push({ ...entry, number: entry.wordNo ?? 0 });
      });
    });
    return all;
  }
  const random10Btn = document.getElementById("random10Btn");

  random10Btn.onclick = () => {
    let allWords = getAllWords();
  
    shuffle(allWords);         // 既存のシャッフル関数使う
    startQuizWithWords(allWords.slice(0, 10)); // 先頭10個だけ取る
  };

  rangeTestBtn?.addEventListener("click", async () => {
    const allWords = getAllWords();
    const maxNumber = allWords.reduce((max, entry) => Math.max(max, entry.number || 0), 0);
    const start = Number.parseInt(rangeStartInput?.value || "", 10);
    const end = Number.parseInt(rangeEndInput?.value || "", 10);

    if (!Number.isInteger(start) || !Number.isInteger(end)) {
      await showAppAlert(`開始番号と終了番号を入力してください（1〜${maxNumber}）。`, "入力エラー");
      return;
    }
    if (start < 1 || end < 1 || start > maxNumber || end > maxNumber) {
      await showAppAlert(`番号は1〜${maxNumber}の範囲で入力してください。`, "入力エラー");
      return;
    }
    if (start > end) {
      await showAppAlert("開始番号は終了番号以下にしてください。", "入力エラー");
      return;
    }

    const selectedWords = allWords.filter(entry => entry.number >= start && entry.number <= end);
    if (selectedWords.length === 0) {
      await showAppAlert("指定範囲に単語がありません。", "お知らせ");
      return;
    }

    startQuizWithWords(selectedWords);
  });
    
  